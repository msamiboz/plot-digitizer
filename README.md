# Plot Digitizer

Extract time-series data from chart images by interactively calibrating axes,
picking a target color region, and computing the median path.

## Install

pip install -r requirements.txt
